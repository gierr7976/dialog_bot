environment_config:
  path: src/gen/config.dart
  class: BotConfig

  fields:
    token:
      config_field: true

    mongo_uri:
      default: mongodb://localhost/dialog_bot

    home_route:
      default: /start

    signup_code_lifetime:
      default: 14
      type: Duration
      pattern: 'Duration(days: __VALUE__)'